node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Local document storage (dev / edge micro-server) - a cloud deploy needs
# object storage instead, since Render's free-tier disk is ephemeral.
/uploads
